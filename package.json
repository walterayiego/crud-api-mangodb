{
  "name": "crud-api-mangodb",
  "version": "1.0.0",
  "description": "A simple CRUD (Create, Read, Update, Delete) API project utilizing MongoDB as the database. Implements basic API endpoints for managing data operations efficiently",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^6.5.0",
    "mongoose": "^8.3.1"
  }
}
